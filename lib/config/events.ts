/**
 * Global custom event names used for cross-component communication.
 */

/**
 * Event triggered to reset the dashboard state (category, search, etc.).
 * Usually triggered by clicking the logo while already on the home page.
 */
export const RESET_DASHBOARD_EVENT = 'reset-dashboard';

/**
 * Event triggered to open the global command menu (search).
 */
export const OPEN_COMMAND_MENU_EVENT = 'open-command-menu';
